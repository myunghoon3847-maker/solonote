# 훈노트 v4.5.6 내부 검수 보고서

## 자동 검사

- JavaScript 문법 검사: 통과
- 보안·회귀 테스트: 7/7 통과
- 로컬 파일 및 서비스워커 참조 검사: 통과
- ZIP 무결성 검사: 배포 압축 후 수행

## 모바일 브라우저 검사

Chromium 모바일 뷰포트에서 다음 너비를 각각 확인했습니다.

- 320px
- 360px
- 375px
- 390px
- 412px
- 430px

확인 화면:

- 로그인 화면
- 메모 메인 화면
- 메모 작성 화면
- 카테고리 관리 모달
- 우측 앱 메뉴

모든 너비와 화면에서 `documentElement.scrollWidth`가 실제 화면 너비와 일치했고, 화면 밖으로 나가는 표시 요소는 검출되지 않았습니다.

## 로고 동작 검사

- 렌더링 요소: `A`
- 브라우저 기본 모양: `appearance: none`
- 할 일 화면 → 로고 클릭 → 메모 홈: 통과
- 작성 화면 → 로고 클릭 → 메모 홈: 통과
- 휴지통 화면 → 로고 클릭 → 메모 홈: 통과
- JavaScript 미실행 시 `href="./"` 홈 이동 대체 동작 존재: 확인

## 이전 캐시 갱신 검사

v4.5.5 서비스워커가 설치된 동일 주소에서 v4.5.6 파일로 교체하는 상황을 재현했습니다.

- 새 HTML의 버전 표시 `v4.5.6`: 확인
- 새 CSS 주소 `v=456`: 확인
- 새 로고가 링크 요소로 변경됨: 확인
- 새 서비스워커 `service-worker.js?v=456`이 업데이트 대기 상태로 설치됨: 확인

## 제한 사항

실제 운영 Supabase 계정의 데이터 쓰기·삭제는 로컬 검수에서 수행하지 않았습니다. 이번 수정은 인증·DB 구조를 변경하지 않습니다.
