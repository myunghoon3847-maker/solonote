# 업무노트 v4.3.2.6 내부 검수 보고서

- 외부 npm import 없음
- OPTIONS가 첫 분기에서 즉시 200 응답
- 브라우저 코드에 관리자 키 없음
- 사용자 ID는 Auth 서버의 `/auth/v1/user` 응답에서만 취득
- 삭제 대상 사용자 ID를 브라우저 요청 본문에서 받지 않음
- 최근 비밀번호 인증 JWT 확인 유지
- CASCADE 안전 점검 성공 전 Auth 삭제 금지
- 메모 선삭제 없음
- Auth 관리자 삭제 요청 본문 `should_soft_delete: false`
- 각 네트워크 단계 개별 제한 시간 적용
- 계정 삭제 성공 후 즉시 200 응답
